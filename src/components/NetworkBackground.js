import React from 'react';

const NetworkBackground = () => {
  return (
    <svg className="fixed top-0 left-0 w-full h-full z-[-1] opacity-[0.03] pointer-events-none">
      {/* Grid pattern, animated lines, and pulsing nodes */}
    </svg>
  );
};
